node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Barra inicial obrigatoria: sem ela, `uploads/` casa em qualquer profundidade e
# tambem exclui src/routes/uploads/ — a rota que serve as artes. Ela sumiu do
# repositorio inteiro assim, e so em producao, porque local o arquivo existia.
/.env
/*.db
/*.db-*
/uploads/

